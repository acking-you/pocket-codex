# Flutter / Dart build outputs
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/

# IDE
.idea/
.vscode/
*.iml

# FVM Version Cache (bound by .fvmrc, not committed)
.fvm/

# Generated by flutter_rust_bridge
# Note: lib/src/rust/ contains generated bindings — those *are*
# committed so downstream contributors don't need to rebuild them.
# Do not ignore that directory.
